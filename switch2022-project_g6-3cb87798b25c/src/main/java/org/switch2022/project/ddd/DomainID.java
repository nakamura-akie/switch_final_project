package org.switch2022.project.ddd;

/**
 * The DomainID interface represents the identifier of a domain entity in the domain-driven design (DDD) approach.
 * It extends the ValueObject interface, indicating that an ID is a value object.
 */
public interface DomainID extends ValueObject {
}
