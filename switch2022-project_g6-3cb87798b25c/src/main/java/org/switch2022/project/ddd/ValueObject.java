package org.switch2022.project.ddd;

/**
 * The ValueObject interface represents a value object in the domain-driven design (DDD) approach.
 * Value objects are immutable objects that contain attributes but have no conceptual identity.
 * They are used to encapsulate and represent a concept or value within the domain model.
 */
public interface ValueObject {
}
